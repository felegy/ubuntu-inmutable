name: container-ci

on:
    push:
        branches:
            - main

jobs:
    build-and-publish:
        runs-on: ubuntu-latest
        env:
            CI: "true"
            IMAGE_NAME: ghcr.io/${{ gitea.repository }}
            BASE_IMAGE: ubuntu:24.04
            KAIROS_INIT_VERSION: 0.7.0
            KAIROS_DEFAULT_USER: kairos
            KAIROS_VARIANT: core
            KAIROS_MODEL: generic
            KAIROS_TRUSTED_BOOT: "false"
            KAIROS_VERSION: 0.3.1
            KUBERNETES_DISTRO: ""
            KUBERNETES_VERSION: ""
            KAIROS_DEBS: open-vm-tools
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: 10.0.x

            - name: Setup Docker Buildx
              run: |
                  docker buildx create --use --name ci-builder || docker buildx use ci-builder
                  docker buildx inspect --bootstrap

            - name: Login to GHCR
              run: echo "${{ secrets.REGISTRY_TOKEN }}" | docker login ghcr.io -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin

            - name: Install Trivy
              run: |
                  sudo apt-get update
                  sudo apt-get install -y wget gnupg lsb-release
                  wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo gpg --dearmor -o /usr/share/keyrings/trivy.gpg
                  echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list
                  sudo apt-get update
                  sudo apt-get install -y trivy

            - name: Run Scripted CI
              run: ./build.sh ci --push true
